# Machine-readable outputs

- `complexity.json`: dense `s=1` operation-count and timing audit;
- `theorem_audits.json`: normality, online covariance, and global convergence checks;
- `initial_verdict.json`: Cayley–Hamilton and special-case checks;
- `pilot.json`: deliberately failed literal empirical pilot;
- `publication_gate.json`: authoritative evidence-release status;
- `SHA256SUMS.txt`: recorded integrity manifest for the original handoff.

`VERIFIED_SCOPED` means the declared theorem-instance contract passed. `NOT_READY` means the paper-level empirical protocol cannot be claimed because its parameter-approximation procedure is not specified or released.
